spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rdv
    username: root
    password:
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
spring-doc:
  default-produces-media-type: application/json
